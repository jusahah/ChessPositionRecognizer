var Promise = require('bluebird');

module.exports = function(featureVectors) {
	return new Promise(function(resolve, reject) {
		var resultFen;
		
		// Do your thing here

		return resolve(resultFen);
	});

}